# Devplan interactive interview — deployed, model authentication blocked

Source: extensions/devplan-interview. Deployment: scripts/deploy_devplan_interview.py, private .runtime/devplan-interview/config.json, managed extension devplan-interview. Separate external-browser workspace window preserves the original memory-only Studio draft. Export from old Studio and import here to migrate.

Unlock with the Orbit host token in the UI (never chat). The service validates against Orbit, discards the token, and issues a four-hour memory-only bearer credential. Exact Origin/Host checks, bounded input, one model turn at a time, no wildcard CORS. No secrets in bundles or URLs. Each turn sends the accepted brief and bounded conversation history to the configured Hermes provider. The model is constructed with enabled_toolsets=[], memory/context/background-review disabled. It proposes structured changes; human acceptance takes a checkpoint and invalidates spec approval. Planning/export remain deterministic, not AI-reviewed. Transcript is included in project export/import. Browser storage may persist the draft; exports are the durable backup. Pending proposals and in-flight turns do not survive reload.

Trusted service runs with owner privileges; model tool disabling is not OS isolation. Hermes runtime may write request diagnostics to its private sessions directory, including project text. Do not claim zero disk retention. Workspace checkpoints cannot restore drafts, model requests or service state. Extension runner has no reboot supervision. Closing the window does not stop the service. Stop through scripts/extensions.py stop devplan-interview and remove the dedicated Tailscale Serve 4361 proxy when no longer needed.

Verification: production build and all 69 Node tests passed. tests/devplan-interview-ui.browser.py passed real unlock, live provider-auth failure and unchanged brief, explicitly synthetic proposal acceptance, real export/import, mobile bounds and external Orbit iframe rendering; zero JS errors. Workspace revision 963 browser acknowledged. tests/devplan-interview.browser.py is the real two-turn success test and has NOT passed: current Codex model returns HTTP 401 token_expired. Standard credential resolution and force-refresh helper did not resolve this (pool-sourced credential). Re-authenticate the configured provider, then rerun the real browser test before claiming conversational success. No mocked response is represented as a successful model turn.
