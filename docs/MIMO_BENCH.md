# MiMo Performance Lab

Owner-only application: https://kimi.tailec998.ts.net:4366/; Orbit window MiMo · Performance Lab. Trusted extension `mimo-bench` uses loopback 4469. Port 4365 belongs to a concurrently deployed service and must not be repointed.

Source: extensions/mimo-bench. Service accesses only MIMO_API_KEY through the existing vault module, server-side. No key in browser, history, errors or command arguments. Fixed HTTPS destination api.xiaomimimo.com, redirects disabled; authenticated owner Tailscale identity and exact Origin required. Local owner/root remain trusted, as with Orbit Secrets. UI served by authenticated backend rather than public static plugin because it needs a private same-origin API.

Model catalog verified against the authenticated provider /v1/models endpoint. Release 1.1.0 supports mimo-v2.6-flash, mimo-v2.6-pro, mimo-v2.6-pro-ultraspeed, mimo-v2.5 and mimo-v2.5-pro, individually or through Compare all text models. Four speech models are listed separately but not benchmarked with text workloads. Catalog is a verified release snapshot, not automatic discovery. Samples are per model (maximum 100 total calls in comparison), concurrency is global; model order rotates per round. Per-model summaries, request budget and model-tagged exports are included. Six unit tests and tests/mimo-compare.browser.py passed; the latter made five real 32-token-cap calls, all successful with exact-match PASS. Build and 70 project tests passed. Prior extension release remains available for code rollback; history and charges are not rolled back. Refresh the existing app to load updated frontend assets.

Model mimo-v2.6-flash verified using the provider model-list API. Workloads: first response with exact match, long-form generation, JSON compliance. 1–20 samples, concurrency 1–4, 32–1024 max completion tokens, thinking on/off. Explicit per-run checkbox consent; no automatic retries or warm-ups. Stop skips queued work; in-flight requests may complete and bill. Do not execute generated content.

Monotonic host timing: first nonempty content/reasoning delta, first answer content, total complete stream. Reported completion tokens divided by total time yields end-to-end tokens/s, NOT decode-only throughput. Missing usage is unavailable. Nearest-rank P50/P95 over successful requests, error rate over all finished requests. JSON/exact-match checks are narrow sanity tests. First-call/network/cache effects included. Pricing is not assumed.

Last 50 runs persist privately under .runtime/mimo-bench/results.json. JSON/CSV export works in top-level browser tab; embedded Orbit browser sandbox restricts downloads, so use the Open securely in a tab link. History is excluded from checkpoints; API spending cannot be rolled back. Extension runner is not a reboot supervisor. Check health with python3 scripts/extensions.py health mimo-bench. If restarting on another loopback port, update only the 4366 Tailscale Serve route. Do not restart Orbit core.

Tests: python3 tests/mimo-bench.test.py (4 tests); .runtime/browser-venv/bin/python tests/mimo-bench.browser.py (runs ONE real billed JSON request; owner auth, invalid Origin, validation, explicit consent, real Orbit iframe sandbox, export, persisted history, no JS errors). npm run check passed build and 70 project tests.

Initial live throughput smoke: 3 sequential requests, thinking disabled, 128 output token cap. All succeeded and hit length cap, provider-reported 384 total output tokens. P50 first output 1619.15 ms, P95 1969.33 ms; P50 total 4698.84 ms; P50 end-to-end throughput 27.24 tokens/s. Separate final live JSON test passed, 14 output tokens, 1992.36 ms first output and 2759.95 ms total. Tiny samples, not a stable performance claim. Window creation acknowledged at revision 1407; iframe render and interactions verified in isolated browser tests.
