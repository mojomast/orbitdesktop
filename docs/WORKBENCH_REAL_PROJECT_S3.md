# Pinned real-project native lane (S3)

Gate 1 was accepted on the merged normal UI in both default and docking renderers before this lane. `tests/workbench-real-project-native.test.mjs` uses a **disposable two-file copy** of the maintained Orbit `agent-profiles` subproject at revision `5b31fdaba1f867eeaf7b0aa30438f096a55b54f6`. It verifies the selected file hashes against `tests/workbench-real-project/manifest.json` when present; it never edits the active source tree. The selected source is 4,725 bytes and its real regression suite is 5,519 bytes. The test executes the baseline, then an actual pinned Hermes/plugin/private-bridge attempt under an explicit owner grant. A fixture loopback endpoint supplies deterministic model responses; this is **not** a live model-evaluation claim.

Blocker classification: both source files and their imports fit the captured-tree limits; no hidden/private inputs, external dependencies, install scripts, network fetches or native build are required. Node builtins suffice. `node-test` discovers and freezes `tests/agent-profiles.test.mjs`, runs the structured recorder on the private candidate, and requires substantive coverage in that file. The owner-declared check has `execution_profile_id:null`: a dependency preparation profile would add no capability for this source-contained project. No output tree is required, and the check does not rely on host ambient `node_modules`. The process is same-UID trusted-host execution, not a sandbox.

The fixture deliberately changes the sessions cap from 100 to 200; its real tests fail. The native agent then repairs it using a source-contained `SESSIONS_PAGE_LIMIT = 100` constant; the same structured tests pass. The owner approves only the exact passing evidence. A private Git unified patch is applied back to the pinned source in a disposable round-trip stage, and the separate artifact verifier re-runs the frozen `node-test` on its own verified copy without altering candidate review evidence. The original two-file source stays byte-identical. The fixture's test tokens are not model credentials.

The runtime accepts `ORBIT_NATIVE_HERMES_API_KEY_FILE` for a same-UID private regular bounded UTF-8 credential. It snapshots the secret at startup, includes only its hash in consent configuration identity, and sends the value to the pinned Hermes adapter only on FD3. Actual owner endpoint selection and live paid-model evaluation are outside this deterministic acceptance fixture; a numeric-loopback HTTP URL is required, but loopback alone says nothing about downstream cost or routing.
